import React from "react";

const Header = () => (
  <header style={{
    background: "#222",
    color: "#fff",
    padding: "1rem 2rem",
    fontSize: "1.5rem",
    fontWeight: "bold",
    letterSpacing: "1px"
  }}>
    Multi-Agent Orchestration Dashboard
  </header>
);

export default Header; 